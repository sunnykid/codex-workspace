# Frontend (React + Vite)

## 실행 방법

```bash
cd frontend
npm install
npm run dev
```

기본 API 주소는 `http://localhost:8000`이며, 다른 주소를 쓰려면 `.env`를 생성하세요.

```bash
VITE_API_BASE_URL=http://localhost:8000
```
